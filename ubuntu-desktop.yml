---
- name: Workstation setup
  hosts: localhost
  connection: local
  become: yes

  vars:
    home: "{{ ansible_env.HOME }}"
    user: "{{ ansible_env.USER }}"

  tasks:

    # ── Základ ────────────────────────────────────────────────
    - name: Vytvoř adresář pro GPG klíče
      file:
        path: /etc/apt/keyrings
        state: directory
        mode: '0755'

    - name: Odstraň zastaralý vscode.list (pokud existuje z dřívější instalace)
      file:
        path: /etc/apt/sources.list.d/vscode.list
        state: absent

    - name: Aktualizace apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Upgrade všech apt balíčků
      apt:
        upgrade: dist

    - name: Instalace apt balíčků
      apt:
        name:
          - mc
          - keepassxc
          - curl
          - git
          - gpg
          - build-essential
          - default-jre
        state: present

    # ── SSH server ────────────────────────────────────────────
    - name: Instalace OpenSSH server
      apt:
        name: openssh-server
        state: present

    - name: Povolení a spuštění SSH
      systemd:
        name: ssh
        enabled: yes
        state: started

    # ── Snap balíčky ──────────────────────────────────────────
    - name: Instalace snap balíčků
      snap:
        name: "{{ item.name }}"
        classic: "{{ item.classic | default(false) }}"
      loop:
        - { name: nvim, classic: true }
        - { name: btop }
        - { name: firefox }
        - { name: remmina }
        - { name: telegram-desktop }
        - { name: onlyoffice-desktopeditors }

    - name: Nastav Firefox jako výchozí prohlížeč
      command: xdg-settings set default-web-browser firefox_firefox.desktop
      become: no
      when: ansible_env.DISPLAY is defined or ansible_env.WAYLAND_DISPLAY is defined

    # ── Microsoft apt repo (VS Code + Edge) ──────────────────
    - name: Microsoft GPG klíč (/usr/share/keyrings - pro VS Code)
      shell: |
        curl -fsSL https://packages.microsoft.com/keys/microsoft.asc | gpg --dearmor > /usr/share/keyrings/microsoft.gpg
      args:
        creates: /usr/share/keyrings/microsoft.gpg

    - name: Microsoft GPG klíč (/etc/apt/keyrings - pro Edge)
      copy:
        src: /usr/share/keyrings/microsoft.gpg
        dest: /etc/apt/keyrings/microsoft.gpg
        remote_src: yes
        mode: '0644'

    - name: VS Code apt repozitář (deb822)
      copy:
        dest: /etc/apt/sources.list.d/vscode.sources
        content: |
          Types: deb
          URIs: https://packages.microsoft.com/repos/code
          Suites: stable
          Components: main
          Architectures: amd64,arm64,armhf
          Signed-By: /usr/share/keyrings/microsoft.gpg

    - name: MS Edge apt repozitář
      apt_repository:
        repo: "deb [arch=amd64 signed-by=/etc/apt/keyrings/microsoft.gpg] https://packages.microsoft.com/repos/edge stable main"
        filename: microsoft-edge

    - name: Instalace VS Code a MS Edge
      apt:
        name:
          - code
          - microsoft-edge-stable
        state: present
        update_cache: yes

    # ── Insync (apt repo) ─────────────────────────────────────
    - name: Insync GPG klíč
      shell: |
        curl -fsSL https://apt.insync.io/insynchq.gpg | gpg --dearmor | tee /etc/apt/trusted.gpg.d/insynchq.gpg > /dev/null
      args:
        creates: /etc/apt/trusted.gpg.d/insynchq.gpg

    - name: Insync apt repozitář
      apt_repository:
        repo: "deb [signed-by=/etc/apt/trusted.gpg.d/insynchq.gpg] http://apt.insync.io/ubuntu {{ ansible_distribution_release }} non-free contrib"
        filename: insync

    - name: Instalace Insync
      apt:
        name:
          - insync
          - insync-nautilus
        state: present
        update_cache: yes

    # ── Tabby Terminal (GitHub releases) ─────────────────────
    - name: Zjisti nejnovější verzi Tabby
      uri:
        url: https://api.github.com/repos/Eugeny/tabby/releases/latest
        return_content: yes
      register: tabby_release
      become: no

    - name: Nainstaluj Tabby .deb
      apt:
        deb: "{{ tabby_release.json.assets \
          | selectattr('name', 'match', '.*linux-x64\\.deb') \
          | map(attribute='browser_download_url') \
          | first }}"

    # ── Claude Code (přes Node.js + npm) ─────────────────────
    - name: NodeSource GPG klíč
      shell: |
        curl -fsSL https://deb.nodesource.com/gpgkey/nodesource-repo.gpg.key | gpg --dearmor -o /etc/apt/keyrings/nodesource.gpg
      args:
        creates: /etc/apt/keyrings/nodesource.gpg

    - name: NodeSource apt repozitář (Node 22)
      apt_repository:
        repo: "deb [signed-by=/etc/apt/keyrings/nodesource.gpg] https://deb.nodesource.com/node_22.x nodistro main"
        filename: nodesource

    - name: Instalace Node.js
      apt:
        name: nodejs
        state: present
        update_cache: yes

    - name: Instalace Claude Code
      npm:
        name: "@anthropic-ai/claude-code"
        global: yes
